# A1 Canvas submission checklist

Before uploading the ZIP to Canvas:

- [ ] `A1.ipynb` was restarted, run from top to bottom, and saved without errors.
- [ ] Every required plot has a useful caption or nearby textual description.
- [ ] `A1.html` was exported **after** the final notebook execution. PDF is not required.
- [ ] `src/student_code.py`, all named tables and figures, the transformed MP4, and one extension artifact are present.
- [ ] Student name, ABC123, assignment/environment versions, external resources, and meaningful generative-AI use are declared.
- [ ] `python scripts/validate_submission.py --assignment A1` reports zero errors.
- [ ] `python scripts/package_submission.py --assignment A1` created `LastName_FirstName_A1.zip`.
- [ ] The ZIP has one top-level folder and contains the notebook, HTML, source, selected outputs, and `validation-report.json`.
- [ ] No datasets, caches, environments, checkpoints, temporary files, or unrelated work are included.

Upload the generated ZIP, not the assignment directory and not a PDF.
